{
  "name": "@services/auth-service",
  "version": "1.0.0",
  "description": "Service for Google OAuth authentication and JWT token generation",
  "main": "dist/handler/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "package": "esbuild src/handler/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js && zip function.zip dist/index.js",
    "deploy": "echo Deploying auth-service..."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.975.0",
    "@aws-sdk/client-secrets-manager": "^3.975.0",
    "@aws-sdk/client-sns": "^3.975.0",
    "@aws-sdk/client-sqs": "^3.990.0",
    "@aws-sdk/lib-dynamodb": "^3.975.0",
    "@libs/domain": "*",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.27.2",
    "google-auth-library": "^9.0.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.0.10",
    "typescript": "^5.9.3"
  }
}
